package ma.digital.prospace.web.rest;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
/*
@RestController
@RequestMapping("/api")
public class EntrepriseController {

 /*   private final JusticeServiceProperties justiceServiceProperties;

    @Autowired
    public EntrepriseController(JusticeServiceProperties justiceServiceProperties) {
        this.justiceServiceProperties = justiceServiceProperties;
    }

    @GetMapping("/entreprise")
    public String getEntrepriseInfo() {
        // Mock endpoint URL for entreprise service
        String entrepriseUrl = justiceServiceProperties.getCompanyBaseUrl() + justiceServiceProperties.getCompanyEndpoint();
        // Here you can perform any logic needed to fetch entreprise info
        // For now, just return the mocked URL
        return "Entreprise info endpoint: " + entrepriseUrl;
    }
}


}*/